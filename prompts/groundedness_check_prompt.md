# Groundedness Check

現在のGroundedness評価はLLMプロンプトを使わず、決定的なルールで評価します。

条件:

- 系列長が2以上4以下。
- 自己ループ `A -> A` が含まれない。
- すべての隣接状態ペアがMarkov graphのエッジとして存在する。
- 各エッジの遷移確率が0.2以上。

将来LLMにgroundedness説明を生成させる場合は、この条件を変更せず、説明生成のみを追加してください。
